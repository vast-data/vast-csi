{{- /*
  Replication CRDs – installed when any replication feature is enabled.
  Auto-generated by "make sync-chart-crds". DO NOT EDIT manually.
*/}}

{{- if include "vastcsi.extension-enabled" . }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: replicationprovisions.vastdata.com
spec:
  group: vastdata.com
  names:
    kind: ReplicationProvision
    listKind: ReplicationProvisionList
    plural: replicationprovisions
    shortNames:
    - rp
    singular: replicationprovision
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provisionerType
      name: Type
      type: string
    - jsonPath: .spec.destinationStorageClass
      name: Dest SC
      type: string
    - jsonPath: .spec.destinationKind
      name: Source Kind
      type: string
    - jsonPath: .spec.originRef.pvcsPreview
      name: Source PVCs
      type: string
    - jsonPath: .spec.replicationState
      name: State
      type: string
    - jsonPath: .status.provisioned
      name: Provisioned
      type: boolean
    - jsonPath: .spec.originRef.replicationPath
      name: Replication Path
      type: string
    - jsonPath: .spec.originRef.protectionPolicyName
      name: Protection Policy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ReplicationProvision is a stable intermediate CRD that holds the full
          provisioning state for one destination StorageClass mirroring a parent
          VolumeReplication or VolumeGroupReplication.

          Lifecycle:

            - ReplicationObjectReconciler creates one ReplicationProvision per
              destination StorageClass, sets an owner reference to the parent ("main")
              VolumeReplication or VolumeGroupReplication, and keeps originRef.pvcs
              up-to-date whenever the parent's membership changes.
            - ReplicationProvisionReconciler provisions the mirrored resources when
              metadata.generation > status.observedGeneration, then records
              ReplicationPath and ProtectionPolicyName in the status so cleanup can
              proceed even if the mirrored objects are already gone.
            - Cleanup is driven by this object's finalizer.  The parent name (taken
              from the owner reference) is used as a label key to find all managed
              resources for that specific destination StorageClass.

          Deletion scenarios:

           1. Delete one ReplicationProvision → cleans only resources for that SC.
           2. Delete the parent VR/VGR → Kubernetes GC cascades to all owned
              ReplicationProvision objects via owner-reference garbage collection.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ReplicationProvisionSpec defines the desired state of ReplicationProvision.
              Fields are managed by the ReplicationObjectReconciler (parent controller).
            properties:
              destinationKind:
                description: |-
                  DestinationKind identifies the kind of the mirrored destination object
                  (VolumeReplication or VolumeGroupReplication).  The name and namespace of
                  the mirrored CRD are the same as this ReplicationProvision's own
                  ObjectMeta.Name/Namespace.
                  Set at creation time by the ReplicationObjectReconciler.
                type: string
              destinationStorageClass:
                description: |-
                  DestinationStorageClass is the name of the destination StorageClass this
                  provision object is responsible for.
                type: string
              originRef:
                description: |-
                  OriginRef is a snapshot of the parent VolumeReplication or
                  VolumeGroupReplication spec fields that the provisioner needs to construct
                  mirrored resources without fetching the parent object.
                properties:
                  autoResync:
                    description: |-
                      AutoResync mirrors the autoResync field from the parent VolumeReplication
                      or VolumeGroupReplication spec.
                    type: boolean
                  protectedPathName:
                    description: |-
                      ProtectedPathName is the name of the VAST protected path (ppath) on the
                      source cluster that this replication tracks.  Resolved at RP creation time
                      by querying the VAST cluster for a ppath whose name ends with the source
                      PV / VolumeReplicationName suffix.
                    type: string
                  protectionPolicyName:
                    description: |-
                      ProtectionPolicyName is the name of the VAST protection policy associated
                      with this replication.  Populated at RP creation time for the same reason
                      as ReplicationPath.
                    type: string
                  pvcs:
                    description: |-
                      PVCs is the list of PVC names from the parent VolumeReplication or
                      VolumeGroupReplication at the time this spec was last written.
                      Updated by the ReplicationObjectReconciler whenever the parent's PVC
                      membership changes (e.g. via ModifyReplicationMembership for a group).
                      A change here bumps the object's metadata.generation, which triggers the
                      ReplicationProvisionReconciler to re-provision the new members.
                    items:
                      type: string
                    type: array
                  pvcsPreview:
                    description: |-
                      PVCsPreview is a human-readable summary of the PVCs list, formatted by
                      PVCList.String() (e.g. `["pvc1", "pvc2" ... +2 more]`).
                      Used by the kubectl printcolumn since JSONPath cannot invoke Go methods.
                    type: string
                  replicationPath:
                    description: |-
                      ReplicationPath is the sourceDir of the protected path (ppath) associated
                      with this replication.  Populated at RP creation time from the VAST cluster
                      so that snapshot cleanup can proceed even after the source object is gone.
                    type: string
                  storageClass:
                    description: |-
                      StorageClass is the name of the source ("master") StorageClass whose PVCs
                      are being replicated.  Stored so provisioners can reference source-cluster
                      parameters without re-fetching the StorageClass.
                    type: string
                  volumeGroup:
                    description: |-
                      VolumeGroup is the VAST volume container on the source cluster.
                      For block StorageClasses this is the value of the "volume_group" parameter;
                      for file StorageClasses it is the value of the "root_export" parameter.
                    type: string
                  volumeGroupReplicationClassName:
                    description: |-
                      VolumeGroupReplicationClassName is the VolumeGroupReplicationClass name
                      from the parent VolumeGroupReplication spec
                      (vgr.Spec.VolumeGroupReplicationClassName).
                      Used only for VolumeGroupReplication-based provisions.
                    type: string
                  volumeReplicationClass:
                    description: |-
                      VolumeReplicationClass is the VolumeReplicationClass name from the parent
                      VolumeReplication spec (vr.Spec.VolumeReplicationClass).
                      Used only for VolumeReplication-based provisions.
                    type: string
                  volumeReplicationClassName:
                    description: |-
                      VolumeReplicationClassName is the VolumeReplicationClass name from the
                      parent VolumeGroupReplication spec (vgr.Spec.VolumeReplicationClassName).
                      Used only for VolumeGroupReplication-based provisions.
                    type: string
                  volumeReplicationName:
                    description: |-
                      VolumeReplicationName is the VolumeReplicationName field from the parent
                      VolumeGroupReplication spec (vgr.Spec.VolumeReplicationName).
                      Used to derive the existing_volume_group suffix when ensuring the
                      VolumeGroupReplicationClass.
                      Used only for VolumeGroupReplication-based provisions.
                    type: string
                required:
                - protectedPathName
                - protectionPolicyName
                - replicationPath
                - storageClass
                - volumeGroup
                type: object
              provisionerType:
                description: |-
                  ProvisionerType indicates whether this provision is managed by the block
                  or file VAST CSI provisioner.  Determined once at creation time from the
                  destination StorageClass (presence of the "subsystem" parameter → Block).
                enum:
                - Block
                - File
                type: string
              replicationState:
                description: |-
                  ReplicationState mirrors the parent VR/VGR's spec.replicationState so
                  the provisioner knows whether this cluster is the active source.
                  Set and updated by the ReplicationObjectReconciler.
                type: string
            required:
            - destinationStorageClass
            - provisionerType
            type: object
          status:
            description: |-
              ReplicationProvisionStatus defines the observed state of ReplicationProvision.
              Fields are managed by the ReplicationProvisionReconciler.
            properties:
              observedGeneration:
                description: |-
                  ObservedGeneration is the metadata.generation of the spec that was last
                  successfully reconciled.  When ObservedGeneration < metadata.generation
                  the reconciler knows the spec has changed and re-runs provisioning.
                format: int64
                type: integer
              provisioned:
                description: |-
                  Provisioned is true once all resources for the destination StorageClass
                  have been successfully created.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}

{{- if include "vastcsi.extension-enabled" . }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vastreplicationcontents.vastdata.com
spec:
  group: vastdata.com
  names:
    kind: VastReplicationContent
    listKind: VastReplicationContentList
    plural: vastreplicationcontents
    shortNames:
    - vrcon
    singular: vastreplicationcontent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provisionerType
      name: Type
      type: string
    - jsonPath: .spec.storageClass
      name: Storage Class
      type: string
    - jsonPath: .spec.kind
      name: Kind
      type: string
    - jsonPath: .status.pvcsPreview
      name: Source PVCs
      type: string
    - jsonPath: .spec.replicationState
      name: State
      type: string
    - jsonPath: .status.provisioned
      name: Provisioned
      type: boolean
    - jsonPath: .spec.replicationPath
      name: Replication Path
      type: string
    - jsonPath: .spec.protectionPolicyNames
      name: Protection Policies
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VastReplicationContent is the internal CRD that holds the full provisioning
          state for one StorageClass mirroring a parent VolumeReplication or
          VolumeGroupReplication.

          Lifecycle:

            - ReplicationObjectReconciler creates one VastReplicationContent per
              StorageClass, sets an owner reference to the parent ("main")
              VolumeReplication or VolumeGroupReplication, and keeps spec.pvcs
              up-to-date whenever the parent's membership changes.
            - VastReplicationContentReconciler provisions the mirrored resources when
              metadata.generation > status.observedGeneration, then records
              ReplicationPath and ProtectionPolicyNames in the status so cleanup can
              proceed even if the mirrored objects are already gone.
            - Cleanup is driven by this object's finalizer.  The parent name (taken
              from the owner reference) is used as a label key to find all managed
              resources for that specific StorageClass.

          Deletion scenarios:

           1. Delete one VastReplicationContent → cleans only resources for that SC.
           2. Delete the parent VR/VGR → Kubernetes GC cascades to all owned
              VastReplicationContent objects via owner-reference garbage collection.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              VastReplicationContentSpec defines the desired state of VastReplicationContent.
              Fields are managed by the ReplicationObjectReconciler (parent controller).

              Most fields are immutable after creation (enforced by the API server via CEL
              validation rules).  Only spec.pvcs and spec.replicationState may change after
              the object is first written.
            properties:
              kind:
                description: |-
                  Kind identifies the kind of the mirrored destination object
                  (VolumeReplication or VolumeGroupReplication).
                  Set at creation time and never changed.
                type: string
                x-kubernetes-validations:
                - message: kind is immutable
                  rule: self == oldSelf
              protectedPathName:
                description: |-
                  ProtectedPathName is the name of the VAST protected path (ppath) on the
                  source cluster.  Resolved at creation time by querying the VAST cluster.
                type: string
                x-kubernetes-validations:
                - message: protectedPathName is immutable
                  rule: self == oldSelf
              protectionPolicyNames:
                description: |-
                  ProtectionPolicyNames lists every operator-created protection policy whose
                  snapshots may exist under replicationPath on this cluster.  Populated at
                  VRC creation from the parent replication topology.
                items:
                  type: string
                type: array
                x-kubernetes-validations:
                - message: protectionPolicyNames is immutable
                  rule: self == oldSelf
              provisionerType:
                description: |-
                  ProvisionerType indicates whether this content is managed by the block
                  or file VAST CSI provisioner.  Determined once at creation time from the
                  destination StorageClass (presence of the "subsystem" parameter → Block).
                enum:
                - Block
                - File
                type: string
                x-kubernetes-validations:
                - message: provisionerType is immutable
                  rule: self == oldSelf
              pvcs:
                description: |-
                  PVCs is the list of PVC names from the parent VolumeReplication or
                  VolumeGroupReplication.  Updated whenever the parent's PVC membership
                  changes; a change here bumps metadata.generation and triggers re-provisioning.
                items:
                  type: string
                type: array
              replicationPath:
                description: |-
                  ReplicationPath is the sourceDir of the VAST protected path (ppath)
                  associated with this replication.  Set at creation time; used by cleanup
                  logic even after the parent object is gone.
                type: string
                x-kubernetes-validations:
                - message: replicationPath is immutable
                  rule: self == oldSelf
              replicationState:
                description: |-
                  ReplicationState mirrors the parent VR/VGR's replicationState so the
                  provisioner knows whether this cluster is the active source.
                  Updated whenever the parent's replication state changes.
                type: string
              storageClass:
                description: |-
                  StorageClass is the name of the StorageClass this content object is
                  responsible for.  Set at creation time and never changed.
                type: string
                x-kubernetes-validations:
                - message: storageClass is immutable
                  rule: self == oldSelf
              syncPVCPV:
                description: |-
                  SyncPVCPV mirrors the same field from the parent
                  VastStorageClassReplication or VastVolumeReplication.
                  Set at creation time and never changed.
                  When true the provisioner creates/deletes static PV+PVC pairs on the
                  destination cluster.
                type: boolean
                x-kubernetes-validations:
                - message: syncPVCPV is immutable
                  rule: self == oldSelf
            required:
            - protectedPathName
            - protectionPolicyNames
            - provisionerType
            - replicationPath
            - storageClass
            type: object
          status:
            description: |-
              VastReplicationContentStatus defines the observed state of VastReplicationContent.
              Fields are managed by the VastReplicationContentReconciler.
            properties:
              observedGeneration:
                description: |-
                  ObservedGeneration is the metadata.generation of the spec that was last
                  successfully reconciled.  When ObservedGeneration < metadata.generation
                  the reconciler knows the spec has changed and re-runs provisioning.
                format: int64
                type: integer
              provisioned:
                description: |-
                  Provisioned is true once all resources for the StorageClass have been
                  successfully created.
                type: boolean
              pvcs:
                description: |-
                  PVCs is the PVC list from the last successfully reconciled spec.
                  Compared against spec.pvcs at the start of each reconcile to compute
                  which PVCs were added (PVCsToCreate) and which were removed (PVCsToDelete).
                items:
                  type: string
                type: array
              pvcsPreview:
                description: |-
                  PVCsPreview is a human-readable summary of spec.pvcs, formatted by
                  PVCList.String() (e.g. `["pvc1", "pvc2" ... +2 more]`).
                  Kept in status so the kubectl printcolumn can display it via a simple
                  JSONPath expression without invoking Go methods.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}

{{- if include "vastcsi.extension-enabled" . }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vaststorageclassreplications.vastdata.com
spec:
  group: vastdata.com
  names:
    kind: VastStorageClassReplication
    listKind: VastStorageClassReplicationList
    plural: vaststorageclassreplications
    shortNames:
    - vscr
    singular: vaststorageclassreplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.storageClassesPreview
      name: Storage Classes
      type: string
    - jsonPath: .spec.primaryStorageClass
      name: Primary SC
      type: string
    - jsonPath: .spec.failoverType
      name: Failover Type
      type: string
    - jsonPath: .status.currentPrimaryStorageClass
      name: Current Primary
      type: string
    - jsonPath: .status.syncStatus
      name: Sync Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VastStorageClassReplication declares a replication group across multiple
          StorageClasses.  The operator creates one NATIVE_REPLICATION ProtectionPolicy
          per directed link (using the pre-existing ReplicationPeer specified in each
          link) and manages the VAST protected path and replication streams.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VastStorageClassReplicationSpec defines the desired state
              of VastStorageClassReplication.
            properties:
              destVolReclaimPolicy:
                default: Retain
                description: |-
                  DestVolReclaimPolicy controls whether destination VAST objects (block
                  volumes or file views/quotas) are deleted when the VSCR is deleted.
                  Defaults to Retain.
                enum:
                - Retain
                - Delete
                type: string
              failoverType:
                default: ungracefulFailover
                description: |-
                  FailoverType is the type of failover to execute when the primary StorageClass
                  changes.  Supported values: ungracefulFailover (default), gracefulFailover.
                enum:
                - ungracefulFailover
                - gracefulFailover
                type: string
              primaryStorageClass:
                description: |-
                  PrimaryStorageClass is the StorageClass that is currently acting as the
                  active (primary) replica.  The VolumeGroupReplication for this class gets
                  replicationState=primary; all others get replicationState=secondary.
                type: string
              protectionPolicyTemplate:
                description: |-
                  ProtectionPolicyTemplate defines the schedule for operator-created
                  NATIVE_REPLICATION protection policies.  The operator creates one policy
                  per topology entry side and uses it as a replication stream on the ppath.
                properties:
                  params:
                    description: Params defines the replication schedule frames.  At
                      least one is required.
                    items:
                      description: |-
                        ProtectionPolicyFrame defines one schedule frame for a VAST ProtectionPolicy.
                        It maps directly to the VAST API "frames" array on a ProtectionPolicy object.
                      properties:
                        every:
                          description: |-
                            Every is the replication interval.
                            Time units: s/S=seconds, m=minutes (lowercase only; M=months),
                            h/H=hours, d/D=days, w/W=weeks, M=months(30d), y/Y=years.
                            Examples: "30s", "15m", "1h", "1d", "1w", "1M", "1y".
                          type: string
                        keepLocal:
                          description: |-
                            KeepLocal is how long local snapshots are retained.
                            Same time units as Every. E.g. "2d", "1w", "1M".
                          type: string
                        keepRemote:
                          description: |-
                            KeepRemote is how long remote snapshots are retained.
                            Same time units as Every. E.g. "7d", "4w", "1M".
                          type: string
                        startAt:
                          description: |-
                            StartAt is an optional time at which the first snapshot is taken.
                            Accepted formats: "YYYY-MM-DD HH:MM:SS", "HH:MM", or RFC-3339.
                            Example: "2025-01-01 02:00:00".
                          type: string
                      required:
                      - every
                      type: object
                    minItems: 1
                    type: array
                required:
                - params
                type: object
              protectionTopology:
                description: |-
                  ProtectionTopology defines the undirected replication mesh.  Each entry
                  connects two clusters bidirectionally via a shared PeerName.  The mesh
                  must be complete: for n distinct clusters exactly n*(n-1)/2 entries are
                  required (one per unordered cluster pair).
                  Example for 2 clusters: 1 entry  (A–B).
                  Example for 3 clusters: 3 entries (A–B, A–C, B–C).
                  Example for 4 clusters: 6 entries (A–B, A–C, A–D, B–C, B–D, C–D).
                  The full StorageClass list is derived automatically from these entries.
                items:
                  description: |-
                    ReplicationTarget describes one undirected replication edge in the mesh.

                    Source and Destination are the two StorageClasses (clusters) connected by
                    this entry.  PeerName is the name of the VAST ReplicationPeer that must
                    exist under the same name on both clusters (each pointing to the other).
                    The operator creates one NATIVE_REPLICATION ProtectionPolicy on each side.

                    PeerName is optional: if omitted the operator discovers it automatically by
                    finding the single peer name shared by both clusters.  If multiple peers are
                    shared between the clusters, PeerName must be set explicitly to disambiguate.
                  properties:
                    destination:
                      description: Destination is the StorageClass of the other cluster
                        in this pair.
                      type: string
                    peerName:
                      description: |-
                        PeerName is the name of the VAST ReplicationPeer that exists on both
                        clusters and establishes bidirectional connectivity between them.
                        Users must pre-create this peer on both VAST management servers.
                        If omitted, the operator discovers it automatically; an error is returned
                        if zero or more than one shared peer is found.
                      type: string
                    source:
                      description: Source is the StorageClass of one cluster in this
                        replication pair.
                      type: string
                  required:
                  - destination
                  - source
                  type: object
                minItems: 1
                type: array
              pvcRemap:
                default: false
                description: PVCRemap controls whether PVCs are remapped to the new
                  primary on failover.
                type: boolean
              resync:
                description: 'Resync is a one-shot trigger: set to true to request
                  an immediate resync.'
                type: boolean
              syncIntervalSeconds:
                description: |-
                  SyncIntervalSeconds is the replication sync interval in seconds.
                  When omitted or zero, the interval is derived from the "every" field of
                  the first protectionPolicyTemplate entry.
                format: int64
                minimum: 0
                type: integer
              syncPVCPV:
                default: true
                description: |-
                  SyncPVCPV controls whether the controller creates/deletes static
                  PV+PVC pairs on each destination cluster.  Defaults to true.
                type: boolean
            required:
            - destVolReclaimPolicy
            - failoverType
            - primaryStorageClass
            - protectionPolicyTemplate
            - protectionTopology
            - pvcRemap
            - syncPVCPV
            type: object
          status:
            description: VastStorageClassReplicationStatus defines the observed state
              of VastStorageClassReplication.
            properties:
              currentPrimaryStorageClass:
                description: |-
                  CurrentPrimaryStorageClass is the StorageClass that is currently primary
                  (may differ from spec.primaryStorageClass while an action is in progress).
                type: string
              lastFailoverType:
                description: LastFailoverType is the most recent failover type that
                  was applied.
                enum:
                - ungracefulFailover
                - gracefulFailover
                type: string
              ppathDirMapping:
                additionalProperties:
                  type: string
                description: |-
                  PpathDirMapping maps every StorageClass name in the constellation to its
                  predicted ppath source directory.  For block StorageClasses the value is
                  the subsystem path joined with volume_group; for file StorageClasses it
                  is the root_export parameter value.  Populated once on the first
                  reconcile and treated as immutable thereafter.
                type: object
              ppathName:
                description: |-
                  PpathName is the VAST protected-path name created by the controller on
                  the primary site.  ONE ppath is created per VSCR; additional policies are
                  added as ReplicationStream objects on that same ppath.  Populated only
                  after the ppath has reached a stable role (SOURCE, DESTINATION, or
                  STANDALONE).  Passed to the CSI plugin via the VRC/VGRC
                  vastdata.com/ppath-name parameter so the plugin can look it up directly
                  without creating a ppath of its own.  SOURCE ↔ DESTINATION switching is
                  the CSI plugin's responsibility.
                type: string
              storageClassesPreview:
                description: |-
                  StorageClassesPreview is a human-readable summary of all StorageClasses
                  (primary + all target SCs), formatted for the kubectl printcolumn.
                type: string
              syncStatus:
                description: |-
                  SyncStatus reflects whether replication for spec.primaryStorageClass has
                  completed its desired state.  "Completed" means the VolumeGroupReplication
                  for the primary StorageClass reports State=Primary; "InProgress" means
                  the transition is still ongoing.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}

{{- if include "vastcsi.extension-enabled" . }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vastvolumereplications.vastdata.com
spec:
  group: vastdata.com
  names:
    kind: VastVolumeReplication
    listKind: VastVolumeReplicationList
    plural: vastvolumereplications
    shortNames:
    - vvr
    singular: vastvolumereplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.volumeName
      name: Volume
      type: string
    - jsonPath: .status.storageClassesPreview
      name: Storage Classes
      type: string
    - jsonPath: .spec.primaryStorageClass
      name: Primary SC
      type: string
    - jsonPath: .spec.failoverType
      name: Failover Type
      type: string
    - jsonPath: .status.currentPrimaryStorageClass
      name: Current Primary
      type: string
    - jsonPath: .status.syncStatus
      name: Sync Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VastVolumeReplication declares per-PVC replication across multiple StorageClasses.
          The operator creates one NATIVE_REPLICATION ProtectionPolicy per directed link
          and manages the VAST protected path, replication streams, VolumeReplicationClasses,
          and one VolumeReplication per StorageClass.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VastVolumeReplicationSpec defines the desired state of VastVolumeReplication.
            properties:
              destVolReclaimPolicy:
                default: Retain
                description: |-
                  DestVolReclaimPolicy controls whether destination VAST objects (block
                  volumes or file views/quotas) are deleted when the VVR is deleted.
                  Defaults to Retain.
                enum:
                - Retain
                - Delete
                type: string
              failoverType:
                default: ungracefulFailover
                description: |-
                  FailoverType is the type of failover to execute when the primary StorageClass
                  changes.  Supported values: ungracefulFailover (default), gracefulFailover.
                enum:
                - ungracefulFailover
                - gracefulFailover
                type: string
              primaryStorageClass:
                description: |-
                  PrimaryStorageClass is the StorageClass that is currently acting as the
                  active (primary) replica.  The VolumeReplication for this class gets
                  replicationState=primary; all others get replicationState=secondary.
                type: string
              protectionPolicyTemplate:
                description: |-
                  ProtectionPolicyTemplate defines the schedule for operator-created
                  NATIVE_REPLICATION protection policies.  The operator creates one policy
                  per topology entry side and uses it as a replication stream on the ppath.
                properties:
                  params:
                    description: Params defines the replication schedule frames.  At
                      least one is required.
                    items:
                      description: |-
                        ProtectionPolicyFrame defines one schedule frame for a VAST ProtectionPolicy.
                        It maps directly to the VAST API "frames" array on a ProtectionPolicy object.
                      properties:
                        every:
                          description: |-
                            Every is the replication interval.
                            Time units: s/S=seconds, m=minutes (lowercase only; M=months),
                            h/H=hours, d/D=days, w/W=weeks, M=months(30d), y/Y=years.
                            Examples: "30s", "15m", "1h", "1d", "1w", "1M", "1y".
                          type: string
                        keepLocal:
                          description: |-
                            KeepLocal is how long local snapshots are retained.
                            Same time units as Every. E.g. "2d", "1w", "1M".
                          type: string
                        keepRemote:
                          description: |-
                            KeepRemote is how long remote snapshots are retained.
                            Same time units as Every. E.g. "7d", "4w", "1M".
                          type: string
                        startAt:
                          description: |-
                            StartAt is an optional time at which the first snapshot is taken.
                            Accepted formats: "YYYY-MM-DD HH:MM:SS", "HH:MM", or RFC-3339.
                            Example: "2025-01-01 02:00:00".
                          type: string
                      required:
                      - every
                      type: object
                    minItems: 1
                    type: array
                required:
                - params
                type: object
              protectionTopology:
                description: |-
                  ProtectionTopology defines the undirected replication mesh.  Each entry
                  connects two clusters bidirectionally via a shared PeerName.  The mesh
                  must be complete: for n distinct clusters exactly n*(n-1)/2 entries are
                  required (one per unordered cluster pair).
                  Example for 2 clusters: 1 entry  (A–B).
                  Example for 3 clusters: 3 entries (A–B, A–C, B–C).
                  The full StorageClass list is derived automatically from these entries.
                items:
                  description: |-
                    ReplicationTarget describes one undirected replication edge in the mesh.

                    Source and Destination are the two StorageClasses (clusters) connected by
                    this entry.  PeerName is the name of the VAST ReplicationPeer that must
                    exist under the same name on both clusters (each pointing to the other).
                    The operator creates one NATIVE_REPLICATION ProtectionPolicy on each side.

                    PeerName is optional: if omitted the operator discovers it automatically by
                    finding the single peer name shared by both clusters.  If multiple peers are
                    shared between the clusters, PeerName must be set explicitly to disambiguate.
                  properties:
                    destination:
                      description: Destination is the StorageClass of the other cluster
                        in this pair.
                      type: string
                    peerName:
                      description: |-
                        PeerName is the name of the VAST ReplicationPeer that exists on both
                        clusters and establishes bidirectional connectivity between them.
                        Users must pre-create this peer on both VAST management servers.
                        If omitted, the operator discovers it automatically; an error is returned
                        if zero or more than one shared peer is found.
                      type: string
                    source:
                      description: Source is the StorageClass of one cluster in this
                        replication pair.
                      type: string
                  required:
                  - destination
                  - source
                  type: object
                minItems: 1
                type: array
              pvcRemap:
                default: false
                description: PVCRemap controls whether PVCs are remapped to the new
                  primary on failover.
                type: boolean
              resync:
                description: 'Resync is a one-shot trigger: set to true to request
                  an immediate resync.'
                type: boolean
              syncIntervalSeconds:
                description: |-
                  SyncIntervalSeconds is the replication sync interval in seconds.
                  When omitted or zero, the interval is derived from the "every" field of
                  the first protectionPolicyTemplate entry.
                format: int64
                minimum: 0
                type: integer
              volumeName:
                description: |-
                  VolumeName is the name of the PVC (in the same namespace) that should be
                  replicated across the listed StorageClasses.
                type: string
            required:
            - destVolReclaimPolicy
            - failoverType
            - primaryStorageClass
            - protectionPolicyTemplate
            - protectionTopology
            - pvcRemap
            - volumeName
            type: object
          status:
            description: VastVolumeReplicationStatus defines the observed state of
              VastVolumeReplication.
            properties:
              currentPrimaryStorageClass:
                description: CurrentPrimaryStorageClass is the StorageClass that is
                  currently primary.
                type: string
              lastFailoverType:
                description: LastFailoverType is the most recent failover type that
                  was applied.
                enum:
                - ungracefulFailover
                - gracefulFailover
                type: string
              ppathDirMapping:
                additionalProperties:
                  type: string
                description: |-
                  PpathDirMapping maps every StorageClass name in the constellation to its
                  predicted ppath source directory.  For block StorageClasses the value is
                  the subsystem path joined with the volume name; for file StorageClasses
                  it is the view path matching the volume name.  Populated once on the
                  first reconcile and treated as immutable thereafter.
                type: object
              ppathName:
                description: |-
                  PpathName is the VAST protected-path name created by the controller on
                  the primary site.  ONE ppath is created per VVR; additional policies are
                  added as ReplicationStream objects on that same ppath.  Populated only
                  after the ppath has reached a stable role (SOURCE, DESTINATION, or
                  STANDALONE).  Passed to the CSI plugin via the VRC
                  vastdata.com/ppath-name parameter so the plugin can look it up directly
                  without creating a ppath of its own.  SOURCE ↔ DESTINATION switching is
                  the CSI plugin's responsibility.
                type: string
              storageClassesPreview:
                description: |-
                  StorageClassesPreview is a human-readable summary of all StorageClasses
                  (primary + all target SCs), formatted for the kubectl printcolumn.
                type: string
              syncStatus:
                description: |-
                  SyncStatus reflects whether replication for spec.primaryStorageClass has
                  completed its desired state.  "Completed" means the VolumeReplication for
                  the primary StorageClass reports State=Primary; "InProgress" means the
                  transition is still ongoing.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
