kind: Service
apiVersion: v1
metadata:
  name: csi-vast-provisioner 
  labels:
    app: csi-vast-provisioner 
spec:
  selector:
    app: csi-vast-provisioner 
  ports:
    - name: dummy
      port: 12345

---
kind: StatefulSet
apiVersion: apps/v1
metadata:
  name: csi-vast-provisioner
spec:
  serviceName: "csi-vast-provisioner"
  replicas: 1
  selector:
    matchLabels:
      app: csi-vast-provisioner
  template:
    metadata:
      labels:
        app: csi-vast-provisioner
    spec:
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - csi-vastplugin
            topologyKey: kubernetes.io/hostname
      serviceAccountName: csi-provisioner
      containers:
        - name: csi-provisioner
          image: quay.io/k8scsi/csi-provisioner:v1.4.0
          args:
            - -v=5
            - --csi-address=/csi/csi.sock
            - --connection-timeout=15s
            - --feature-gates=Topology=true
          volumeMounts:
            - mountPath: /csi
              name: socket-dir
      volumes:
        - hostPath:
            path: /var/lib/kubelet/plugins/csi-vast
            type: DirectoryOrCreate
          name: socket-dir
