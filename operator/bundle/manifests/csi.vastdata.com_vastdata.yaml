apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: vastdata.csi.vastdata.com
spec:
  group: csi.vastdata.com
  names:
    kind: VastData
    listKind: VastDataList
    plural: vastdata
    singular: vastdata
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: VastData is the Schema for the vastdata API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: VastDataSpec defines the desired state of VastData
          properties:
            enableSSL:
              default: "false"
              description: Enable/disable SSL termination
              type: boolean
            mountOptions:
              default: ""
              description: Options for mounting storage
              type: string
            nfsExport:
              default: /k8s
              description: Type of NFS Export
              type: string
            pullPolicy:
              description: 'Policy for if/when to pull a CSI driver container image
                Options are: Never, Always, IfNotPresent'
              enum:
              - Always
              - Never
              - IfNotPresent
              type: string
            vipPool:
              default: vippool-1
              description: Pool for Vip
              type: string
            vmsHost:
              type: string
          type: object
        status:
          description: VastDataStatus defines the observed state of VastData
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
