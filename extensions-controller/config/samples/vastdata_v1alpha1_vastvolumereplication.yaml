apiVersion: vastdata.vastdata.com/v1alpha1
kind: VastVolumeReplication
metadata:
  labels:
    app.kubernetes.io/name: extensions-controller
    app.kubernetes.io/managed-by: kustomize
  name: vastvolumereplication-sample
spec:
  # TODO(user): Add fields here
