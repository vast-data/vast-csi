$import object.vproto
$import plasma/vmsg/vmsg.vproto as VMsg

$const P::byte ENV_MAX_SILOS = 32

struct Silo {
    @0 affinity byte;
};

struct EnvProto {
    @0 base_proto ObjectBaseProto;
    @1 id VMsg.EnvId;
    @2 connected bool;
    @3 silos Silo[ENV_MAX_SILOS];
};
