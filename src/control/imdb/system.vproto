$import object.vproto

enum SystemState : byte {
    INIT,
    ONLINE,
    OFFLINE
};

struct SystemProto {
    @0 base_proto ObjectBaseProto;

    @1 next_cnode_id uint16_t;
    @2 next_env_id uint16_t;

    @3 state SystemState;
};
