---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-replicated-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: vastdata-filesystem

