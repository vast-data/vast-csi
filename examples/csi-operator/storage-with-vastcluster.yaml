apiVersion: storage.vastdata.com/v1
kind: VastStorage
metadata:
  name: vastdata-filesystem
  namespace: vast-csi
spec:
  driverType: "block"
  driverName: "block.csi.vastdata.com"
  clusterName: vastcluster
  subsystem: "myblock"
  vipPool: "vippool-1"
  createSnapshotClass: false
