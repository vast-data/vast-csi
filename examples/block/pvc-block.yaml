apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-block
spec:
  volumeMode: Block # or 'FileSystem', which will allow passing the **standard** fsType
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: vastdata-block
